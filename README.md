# Meu CDC — Basic Data

React Native/Expo implementation of the supplied Figma frame.

## Run

```bash
npm install
npm run start
```

The back and continue handlers are intentionally left as integration points.
